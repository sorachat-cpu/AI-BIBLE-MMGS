# templates/

Placeholder directory referenced by `02_ARCHITECTURE.md`'s repository layout.

Intended contents once Phase 4 (Render & Publish) starts:
- Render Engine layout templates (Overlay position presets per aspect ratio / style_tag)
- n8n workflow export JSON files (WF-01 through WF-04, WF-ERR-01), once the stack decision in `20_ROADMAP.md` Phase 0 is made

Prompt templates already have a dedicated home and must NOT be duplicated here -- see `17_PROMPT_LIBRARY.md` and the `TPL_*` naming convention.
