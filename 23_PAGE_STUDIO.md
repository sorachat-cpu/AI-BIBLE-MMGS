# 23. Page Studio — แผนขยายคอนโซลให้จัดการเพจได้ครบวงจร

> Version: 0.1.0 (ตั้งต้นจากไอเดียที่คุยกันวันที่ 2 ส.ค. 2026)
> Status: **ยังไม่ได้เริ่มเขียนโค้ด** — ไฟล์นี้คือสเปกไว้ทำต่อ
> Owner: Architecture & AI Engineering Team
> เกี่ยวข้องกับ: `public/nav.js` (แท็บ) · `14_PUBLISH_ENGINE.md` · `12_RENDER_ENGINE.md` · `20_ROADMAP.md`

---

## 0. ทำไมต้องมีไฟล์นี้

คอนโซลตอนนี้ทำได้ 3 อย่าง: รัน engine · จัดคิวโพสต์ · ตรวจสถานะ ซึ่งครอบคลุม
"ผลิตคลิปแล้วโพสต์" ได้จบ แต่**ไม่ครอบคลุมการดูแลเพจจริงๆ** ที่ต้องมีคอนเทนต์หลายแบบ
ต้องตัดต่อเอง และต้องมีเครื่องมือเฉพาะทางของงานขายที่ดิน

ไฟล์นี้เก็บ 3 ไอเดียหลักไว้เป็นสเปก เพื่อให้หยิบไปทำต่อทีละส่วนได้โดยไม่ต้องรื้อความคิดใหม่

**สถานะแท็บใน `public/nav.js` ตอนนี้:**

| แท็บ | สถานะ | หัวข้อในไฟล์นี้ |
|---|---|---|
| สายพานผลิต · คิวโพสต์ · สถานะระบบ | ใช้งานได้ | — |
| คลังคอนเทนต์ | เร็วๆ นี้ | §1 |
| ตัดต่อ + ซับ | เร็วๆ นี้ | §2 |
| ตีเส้นแปลง | เร็วๆ นี้ | §3 |

> แท็บที่ยังไม่เสร็จ **แสดงแต่กดไม่ได้** และติดป้าย "เร็วๆ นี้" ตั้งใจให้เป็นแบบนี้ —
> แท็บที่กดแล้วไม่ไปไหนทำให้คนเลิกเชื่อถือทั้งแถบ ส่วนแท็บที่บอกตรงๆ ว่ายังไม่เสร็จคือ roadmap ที่อ่านได้

---

## 1. คลังคอนเทนต์ — เลิกให้เพจมีแต่คลิป

### ปัญหา
ตอนนี้คิวมี 2 แบบคือคลิปขายที่ดินกับ carousel ความรู้ ถ้าปล่อยยาว 3 เดือนเพจจะซ้ำจนคนเลื่อนผ่าน
และคลิปในคลังมีแค่ 7 ตัว หมุนได้ประมาณ 7 วันก่อนเริ่มซ้ำแปลงเดิม

### ประเภทคอนเทนต์ที่ควรเพิ่ม

| ชนิด | ทำอะไร | ต้นทุน | ทำไมถึงคุ้ม |
|---|---|---|---|
| **ก่อน/หลัง ถมที่** | สไลด์เทียบภาพเดียวกันสองช่วงเวลา | ฟรี (มีภาพอยู่แล้ว) | คนดูจบสูง เห็นผลลัพธ์ชัด |
| **ถาม-ตอบจากคอมเมนต์** | ดึงคำถามจริงมาตอบเป็นสไลด์ | ฟรี | คนถามซ้ำๆ อยู่แล้ว ตอบครั้งเดียวใช้ยาว |
| **ราคาที่ดินรายตำบล** | กราฟ/ตารางจากชีทที่มี | ฟรี | ของที่คู่แข่งไม่มี ทำให้เพจเป็นแหล่งอ้างอิง |
| **รีวิวเส้นทาง** | คลิปขับรถเข้าแปลง + ซับบอกระยะ | ถ่ายเอง | ตอบคำถามที่ถูกถามมากที่สุด "เข้าไปยังไง" |
| **อัปเดตโครงการรอบข้าง** | ถนนตัดใหม่ ห้างมาลง | ฟรี | สร้างเหตุผลให้รีบซื้อโดยไม่ต้องเคลม |
| **เบื้องหลังการทำงาน** | รูปตอนไปวัดที่ ตรวจโฉนด | ฟรี | สร้างความน่าเชื่อถือ ซึ่งขายที่ดินต้องใช้มาก |

### สิ่งที่ต้องสร้าง
- `src/content/formats.mjs` — ทะเบียนชนิดคอนเทนต์ (คู่ขนานกับ `platforms.mjs`)
  แต่ละชนิดบอก: ต้องใช้ input อะไร · เรนเดอร์ด้วย engine ไหน · ลงช่องไหนได้
- ขยาย `schedule.config.json` ให้ slot ระบุ `format` ได้ ไม่ใช่แค่ `content_type: video|image`
- หน้า **คลังคอนเทนต์**: อัปโหลดรูป/คลิปดิบ ติดแท็ก เลือกว่าจะให้เป็นคอนเทนต์ชนิดไหน
- ตัวจัดคิวต้อง**สลับชนิด**ไม่ให้ซ้ำติดกัน (มีตรรกะ cooldown ใน `stock.mjs` อยู่แล้ว ต่อยอดได้)

---

## 2. ตัดต่อขั้นต้น — ตัดช่วงว่าง ใส่ซับ แล้วทิ้งไว้ในโฟลเดอร์

> **ตัดสินใจแล้ว (2 ส.ค. 2026): ไม่ผูกกับ CapCut เลย**
> ไม่มีการอ่าน/เขียนไฟล์โปรเจกต์ CapCut ไม่มีการ import กลับเข้าระบบ
> ระบบทำงานหยาบให้เสร็จ แล้ว **export ทิ้งไว้ในโฟลเดอร์** คนไปเปิดใน CapCut เอง
> ข้อดีคือไม่ต้องแตะรูปแบบไฟล์ภายในที่ไม่มีเอกสารและเปลี่ยนทุกเวอร์ชัน — ไม่มีอะไรให้พัง

### ขอบเขต: ทำแค่ 3 อย่าง
1. **ตัดช่วงว่างออก** — ตัดท่อนเงียบ/นิ่งหัวท้ายและระหว่างประโยค
2. **ใส่ซับ** — จากสคริปต์ที่มีอยู่แล้ว
3. **export ลงโฟลเดอร์** — จบตรงนี้ ไม่ส่งต่อไปไหน

### วิธี
- ตัดช่วงเงียบด้วย FFmpeg `silencedetect` → ได้ช่วงเวลาที่เงียบ → ตัดออกแล้วต่อกลับ
  (มี `ffmpeg-static` และ `src/lib/ffmpeg.mjs` อยู่แล้ว ไม่ต้องลงอะไรเพิ่ม)
- ซับใช้ `src/lib/ass.mjs` ที่มีอยู่ แล้วเขียนออกเป็น `.srt` ด้วย เพราะโปรแกรมตัดต่อทุกตัวอ่าน `.srt` ได้
- **สำคัญ: เวลาซับต้องเลื่อนตามช่วงที่ถูกตัดออก** ไม่งั้นซับจะหลุดกับภาพทั้งคลิป
- เผื่อทางถอย: เก็บไฟล์ดิบไว้ด้วยเสมอ เผื่อการตัดอัตโนมัติกินเนื้อหาที่ต้องการ

### ผลลัพธ์ที่ต้องได้
```
output/edits/{property_id}/
  cut.mp4          คลิปที่ตัดช่วงว่างออกแล้ว
  subtitle.srt     ซับที่เวลาตรงกับ cut.mp4
  original.mp4     ไฟล์ดิบ เผื่อตัดพลาด
  report.txt       ตัดออกกี่ช่วง รวมกี่วินาที
```

### สิ่งที่ต้องสร้าง
- `src/engines/edit-engine.mjs` — silencedetect → ตัด → ต่อ → เลื่อนเวลาซับ → เขียนโฟลเดอร์
- `src/lib/srt.mjs` — เขียน `.srt` + ฟังก์ชันเลื่อนเวลาตามช่วงที่ตัด
- หน้า **ตัดต่อ + ซับ**: อัปโหลดคลิป ตั้งความไวการตัด กดรัน แล้วบอกว่าไฟล์อยู่ที่ไหน

---

## 3. ตีเส้นแปลง — เครื่องมือลากขอบเขตบนภาพมุมสูง

### สิ่งที่อยากได้
อัปโหลดภาพถ่ายโดรน แล้วลากเส้นรอบแปลงที่ดิน ให้เส้น**วิ่งไล่ไปตามขอบ**เหมือน
เครื่องมือใน After Effects / Mocha ที่นักตัดต่อชอบใช้

### แยกให้ชัดก่อนว่าจะทำอันไหน — สองอย่างนี้ยากคนละระดับ

| แบบ | คืออะไร | ความยาก | คุ้มไหม |
|---|---|---|---|
| **A. ภาพนิ่ง + เส้นวิ่ง** | ลากรูปหลายเหลี่ยมบนภาพนิ่ง แล้วทำอนิเมชันให้เส้นวิ่งไปตามขอบ | ต่ำ | **คุ้มที่สุด เริ่มที่นี่** |
| **B. ติดตามในวิดีโอ** | เส้นเกาะติดพื้นที่ขณะกล้องเคลื่อน (planar tracking แบบ Mocha) | สูงมาก | ยังไม่คุ้ม |

**แบบ A ให้ผลทางสายตาเกือบเท่าแบบ B** สำหรับงานขายที่ดิน เพราะภาพโดรนมุมสูงส่วนใหญ่นิ่งอยู่แล้ว
หรือเคลื่อนช้ามาก และเราคุมได้ว่าจะถ่ายแบบไหน

### แบบ A — ออกแบบไว้แล้ว ทำได้เลย

**หน้าเว็บ**
- อัปโหลดภาพ → คลิกวางจุดทีละมุมของแปลง → ปิดรูปหลายเหลี่ยม
- ลากจุดแก้ได้ · กด `⌫` ลบจุดล่าสุด · zoom เพื่อวางจุดให้แม่น
- ตัวช่วยเล็งขอบ: snap เข้าหาเส้นขอบในภาพด้วย Sobel edge detection บน `<canvas>`
  ทำได้ในเบราว์เซอร์ล้วน ไม่ต้องมี dependency
- กรอกพื้นที่จริง (ไร่-งาน-วา) แล้วระบบคำนวณ**มาตราส่วน** เอาไปแสดงความยาวด้านแต่ละด้านได้

**อนิเมชันเส้นวิ่งรอบแปลง — ทำได้ และเป็นส่วนที่ง่ายที่สุดของงานนี้**

ถามไว้ว่า "มีเส้นแล้วทำให้ค่อยๆ เคลื่อนจนวนครบพื้นที่ได้ไหม" — ได้ วิธีมาตรฐานคือ:

- วัดความยาวเส้นรอบรูป `L` แล้วตั้ง `stroke-dasharray: L` กับ `stroke-dashoffset: L`
  → เส้นถูกซ่อนทั้งเส้น
- ไล่ `stroke-dashoffset` จาก `L` ลงไป `0` → เส้นค่อยๆ งอกจากจุดเริ่มไปจนบรรจบครบวง
- ความยาวเส้นหาได้ตรงๆ ด้วย `path.getTotalLength()` ในเบราว์เซอร์

**แต่ตอน export เป็นวิดีโอต้องระวัง**: FFmpeg ไม่เล่น SMIL/CSS animation ให้
วิธีที่ตรงไปตรงมาที่สุดคือ **เรนเดอร์ทีละเฟรมเป็น PNG** (เฟรมที่ `n` ตั้ง `dashoffset`
เป็น `L × (1 − n/N)`) แล้วให้ FFmpeg ต่อเป็นคลิป overlay โปร่งใส
ทางนี้ตรงกับที่ `overlay-engine.mjs` ทำอยู่แล้ว ไม่ต้องมี headless browser

ลูกเล่นที่ได้ฟรีเมื่อมีโครงนี้แล้ว: หยุดค้างที่มุมแต่ละมุมเพื่อขึ้นความยาวด้าน ·
วิ่งครบแล้วค่อยเติมสีพื้นในแปลง · เรืองแสงตามหัวเส้นที่กำลังวิ่ง

**ขาออก**
- PNG โปร่งใสต่อเฟรม → คลิป overlay ให้ FFmpeg ซ้อนบนภาพ (ทางเดิมกับ `overlay-engine.mjs`)
- ถ้าต้องการภาพนิ่งอย่างเดียว ใช้ SVG `<animate>` ตรงๆ ได้เลย ไม่ต้องเรนเดอร์ทีละเฟรม
- เก็บพิกัดจุดไว้ใน `content/parcels/{property_id}.json` เพื่อกลับมาแก้ทีหลังได้
  โดยไม่ต้องลากใหม่ — **ข้อนี้สำคัญ** เพราะแปลงเดิมถูกโพสต์ซ้ำหลายรอบ

**สิ่งที่ต้องสร้าง**
- `public/parcel.html` — หน้าลากเส้น
- `src/engines/parcel-engine.mjs` — พิกัด → SVG → PNG
- `POST /api/parcel` — บันทึกและเรนเดอร์

### แบบ B — เก็บไว้ก่อน
ถ้าจะทำจริงต้องใช้ optical flow หรือ homography ต่อเฟรม ซึ่งเกินขอบเขต Node ล้วน
ทางที่สมเหตุสมผลกว่าคือ**ให้ CapCut/AE ทำส่วนนี้** แล้วเราส่ง overlay จากแบบ A ไปให้ (§2)

---

## 3.5 ศูนย์รวมโพสต์และแชท — "กดทีเดียว ออกทุกแอพ"

> เพิ่มไว้ 3 ส.ค. 2569 · **ยังไม่ได้เริ่ม** · เป็นไอเดียที่ user อยากทำต่อรอบหน้า

### สิ่งที่อยากได้
1. **กดโพสต์ครั้งเดียว ออกทุกแพลตฟอร์ม** — Facebook · TikTok · YouTube · Instagram · LINE
2. **รวมแชททุกแอพไว้ที่เดียว** ไม่ต้องสลับแอพไปมา

### ข้อ 1 — โครงมีอยู่แล้วเกือบครบ
`publish-engine.mjs` ยิงหลายปลายทางพร้อมกันได้ตั้งแต่แรก และหน้า `/publish` ก็มีปุ่มเดียวที่ยิงทุกช่องอยู่แล้ว
**สิ่งที่ขาดคือ credential ของแต่ละแพลตฟอร์ม ไม่ใช่โค้ด** — ใส่ค่าใน `.env` แล้วใช้ได้ทันที

| ปลายทาง | ขาดอะไร | ยากแค่ไหน |
|---|---|---|
| Facebook Page / Reels | ต่อแล้ว แต่**ติดโหมด Development ของแอป** (ดู `20_ROADMAP.md`) | ติดที่ Dashboard ไม่ใช่โค้ด |
| TikTok | `TIKTOK_ACCESS_TOKEN` · ต้องสมัคร developer + ผ่านรีวิว | ปานกลาง |
| YouTube Shorts | OAuth 3 ค่า · ต้องเปิด Data API v3 ใน Google Cloud | ปานกลาง |
| Instagram Reels | ต้องมี IG Business + `PUBLIC_MEDIA_BASE_URL` (IG ดึงไฟล์จาก URL เท่านั้น อัปโหลดตรงไม่ได้) | ต้องมีที่ฝากไฟล์สาธารณะ |
| LINE OA | ต่อแล้ว ขาดแค่ที่ฝากไฟล์ | ง่าย |

**ตัวติดร่วมของ IG กับ LINE คือที่ฝากไฟล์สาธารณะ** ทำครั้งเดียวได้ทั้งสองช่อง

### ข้อ 2 — รวมแชท: ต้องรู้ข้อจำกัดก่อนวางแผน
อันนี้**ไม่ได้ทำได้ทุกแอพ** และไม่ใช่เพราะเขียนโค้ดไม่ไหว แต่เพราะเจ้าของแพลตฟอร์มไม่เปิดให้

| แอพ | อ่าน/ตอบแชทผ่าน API ได้ไหม |
|---|---|
| **Facebook Messenger** | ✅ ได้ · Messenger Platform + `pages_messaging` (ต้องผ่าน App Review) |
| **Instagram DM** | ✅ ได้ · ผ่าน Messenger API for Instagram (ต้องเป็น IG Business) |
| **LINE OA** | ✅ ได้ · Messaging API + webhook (ง่ายที่สุด มี token อยู่แล้ว) |
| **TikTok** | ❌ **ไม่มี API สำหรับ DM** |
| **YouTube** | ⚠️ ได้แค่คอมเมนต์ ไม่ใช่แชท |

**สรุป: รวมได้จริง 3 จาก 5** ถ้าจะทำควรบอกตัวเองแต่แรกว่าเป้าหมายคือ "รวม Messenger + IG + LINE"
ไม่ใช่ "รวมทุกแอพ" ไม่งั้นจะเสียเวลาไล่หา API ที่ไม่มีอยู่จริง

### สิ่งที่ต้องสร้าง (ถ้าเอาจริง)
- `src/inbox/` — อะแดปเตอร์ต่อแพลตฟอร์ม แบบเดียวกับ `src/publish/adapters/`
- **ต้องมี webhook เข้าถึงจากภายนอกได้** ทั้ง Messenger และ LINE ส่งข้อความเข้ามาทาง webhook
  ไม่มีทางดึงย้อนหลังแบบ polling → **ต้องมีเซิร์ฟเวอร์สาธารณะ ไม่ใช่รันบนเครื่องอย่างเดียวเหมือนตอนนี้**
- หน้า **กล่องข้อความ** ในคอนโซล: รายการแชทซ้าย บทสนทนาขวา ติดป้ายว่ามาจากแอพไหน
- ที่เก็บข้อความ — ตอนนี้ระบบใช้ไฟล์ JSON ล้วน ถ้าเก็บแชทด้วยควรขยับไป SQLite

### ข้อควรระวัง
- **หน้าต่าง 24 ชั่วโมง**: ทั้ง Messenger และ IG ให้ตอบข้อความทั่วไปได้ภายใน 24 ชม. หลังผู้ใช้ทัก เกินกว่านั้นส่งได้เฉพาะข้อความตามแม่แบบที่อนุมัติแล้ว — กติกานี้ต้องอยู่ในระบบตั้งแต่ออกแบบ ไม่ใช่มาแปะทีหลัง
- `pages_messaging` **ต้องผ่าน App Review** ต่างจาก `pages_manage_posts` ที่ใช้กับเพจตัวเองได้เลย
- ถ้าจะทำเป็นสินค้าขายจริง การอ่านแชทลูกค้าเข้าข่ายข้อมูลส่วนบุคคล ต้องคิดเรื่อง PDPA ด้วย

---

## 3.6 LINE OA เป็นบอทเต็มรูป — เมนู, CRM, นัดหมาย, แจ้งเตือนข้ามแพลตฟอร์ม

> เพิ่มไว้ 12 ส.ค. 2569 — user สั่งมาเป็นก้อนเดียว ไม่มีที่ไหนในสเปกเดิมพูดถึงมาก่อนเลย
> ยกเว้นข้อคำนวณค่าโอน/ภาษี ซึ่ง **ทำไปแล้ว** (ดูหมายเหตุท้ายข้อ 4)
> **ยังไม่ได้เริ่มเขียนโค้ดข้อไหนในหัวข้อนี้เลย** ทุกข้อยังเป็นสเปก

### สิ่งที่ user ต้องการ (แตกเป็นก้อนย่อย)

1. **Rich Menu มีปุ่มให้เลือก**: ฝากขาย / ขายฝาก / เลือกชมที่ / คำนวณค่าโอน+เช็คเลขโฉนดลิงก์ไป Land Maps
2. **CRM ลูกค้า**: เก็บ ชื่อ · เบอร์ · แปลงที่สนใจ เพื่อให้ทีมติดต่อกลับได้
3. **ตารางนัดหมาย**: เมื่อมีลูกค้านัดชมที่ ให้บันทึกวัน-เวลาแล้ว sync เข้า Google Calendar
4. **แจ้งเตือนข้ามแพลตฟอร์ม**: โพสต์ที่ยิงเข้า Facebook ให้ยิงแจ้งเตือนเข้า LINE ของคนที่เป็นเพื่อนด้วย ต่อยอดไป TikTok/YouTube ได้ในอนาคต
5. **ศูนย์คำนวณค่าโอน/ภาษีที่ดิน** อ้างอิงกรมที่ดิน/ธนาคาร ปีล่าสุด — **✅ มีแล้ว ไม่ต้องทำใหม่**: `src/lib/land-tax.mjs` (เขียนเสร็จ 11 ส.ค., 11/11 เทสต์ผ่าน) มี `calculateTransferCosts()` ครบทั้งค่าโอน/ค่าจำนอง/อากรแสตมป์-ภาษีธุรกิจเฉพาะ/ภาษีหัก ณ ที่จ่าย พร้อม source อ้างอิง — **ที่ยังไม่มีคือช่องทางให้ลูกค้าเรียกใช้เอง** (ตอนนี้เป็นแค่ฟังก์ชันในโค้ด ไม่มี UI/บอทหน้าบ้าน) ข้อ 1 กับข้อนี้จึงเป็นงานเดียวกันครึ่งหนึ่ง: rich menu ปุ่ม "คำนวณ" ก็คือหน้าบ้านของฟังก์ชันนี้

### แต่ละก้อนติดอะไรอยู่ (แยกตามจริง ไม่ใช่ตามที่อยากให้ง่าย)

| ก้อน | มีอะไรอยู่แล้ว | ที่ต้องสร้างใหม่ | ตัวบล็อกจริง |
|---|---|---|---|
| Rich Menu + ปุ่ม | `LINE_CHANNEL_ACCESS_TOKEN` ใช้งานได้จริง · LINE Messaging API มี endpoint `/v2/bot/richmenu` พร้อมใช้ | ภาพเมนู + ผูก postback action ต่อปุ่ม | ไม่มี — ทำได้เลยฝั่ง "ส่งเมนูออกไป" |
| **รับการกดปุ่ม** (postback event) | ไม่มี | `src/inbox/line-webhook.mjs` รับ `POST` จาก LINE | **ต้องมี webhook เข้าถึงจากอินเทอร์เน็ตได้ — เหมือนที่บันทึกไว้ใน §3.5 เป๊ะ** ระบบตอนนี้รันบนเครื่อง local เท่านั้น ยังไม่มีเซิร์ฟเวอร์สาธารณะ นี่คือตัวบล็อกร่วมของทั้งฝากขาย/ขายฝาก/เลือกชมที่/คำนวณ เพราะทั้ง 4 ปุ่มต้องอ่าน event ที่ผู้ใช้กดกลับมา |
| CRM (ชื่อ/เบอร์/แปลงที่สนใจ) | ไม่มีเลย ระบบเก็บแต่ข้อมูลแปลง (`content/listings.json`) ไม่มีตารางลูกค้า | `content/customers.json` (พักไว้ก่อน Postgres แบบเดียวกับ listings/queue) + ฟังก์ชันบันทึก/ค้นหา | ไม่มีตัวบล็อกภายนอก แต่ **ต้องมี webhook ก่อนถึงจะรับข้อมูลจากแชทได้** — ถ้าจะกรอกเองผ่านหน้าเว็บก่อนก็ทำได้โดยไม่ต้องรอ webhook |
| นัดหมาย → Google Calendar | ไม่มีเลย เคยต่อ Google แค่ Maps/Places API เท่านั้น | ต่อ Google Calendar API (คนละ credential จาก Maps API key) | ต้องขอ OAuth consent + refresh token ของบัญชี Google ที่จะใช้เป็นปฏิทิน (ไม่ใช่แค่ API key แบบ Maps) — ยังไม่มีของนี้เลยสักตัว |
| FB → LINE แจ้งเตือน | **โครงมีอยู่แล้ว** — `publish-engine.mjs` ยิงหลายปลายทางพร้อมกันอยู่แล้ว (ดู §3.5 ข้อ 1) LINE adapter ก็เขียนเสร็จแล้ว | แค่ตั้งให้ทุกโพสต์ยิง LINE broadcast คู่กับ Facebook เป็นค่าเริ่มต้น | ถ้าจะแนบรูป/วิดีโอไปด้วยยังติด `PUBLIC_MEDIA_BASE_URL` เดิม (ข้อความล้วนทำได้ตอนนี้เลยไม่ต้องรอ) |
| ต่อยอด TikTok/YouTube | โครง adapter มีสเตอร์ไว้แล้ว | ใส่ credential จริง | เดิมอยู่แล้ว (`TIKTOK_ACCESS_TOKEN`, YouTube OAuth 3 ค่า) ไม่ใช่บล็อกใหม่จากงานนี้ |
| เช็คเลขโฉนด → ลิงก์ Land Maps | ไม่มี | ฟังก์ชันสร้างลิงก์จากเลขโฉนด/พิกัด | **ยังไม่ยืนยันว่า landsmaps.dol.go.th เปิด deep-link ค้นด้วยเลขโฉนดแบบ URL parameter ให้จริงไหม** ต้องตรวจสเปกของเว็บนั้นก่อนสร้างลิงก์ ไม่เดาเอา — ระหว่างนี้ทำแบบ fallback ได้คือลิงก์ไปหน้าแรกของเว็บพร้อมบอกเลขโฉนดให้ลูกค้าพิมพ์เอง |

### สรุปเส้นทางที่เร็วที่สุดถ้าจะเริ่ม

**ทำได้เลยไม่ต้องรอ infra ใหม่**: FB→LINE แจ้งเตือนแบบข้อความล้วน (แค่เพิ่ม LINE เป็นปลายทางเริ่มต้นในคิวโพสต์) และหน้าเว็บกรอก CRM มือ (ยังไม่ต้องมี webhook)

**ต้องมีเซิร์ฟเวอร์สาธารณะก่อนจึงจะเริ่มได้**: Rich Menu ที่ตอบกลับจริง, รับข้อมูลจากแชทเข้า CRM อัตโนมัติ — นี่คือก้อนเดียวกับ §3.5 ที่บอกไว้แล้วว่าเป็นก้อนใหญ่สุด

**ต้องขอ credential ใหม่ที่ยังไม่มีเลยสักตัว**: Google Calendar OAuth (คนละตัวกับ Maps API key ที่มีอยู่)

---

## 4. ลำดับที่ควรทำ

1. **§2 ตัดต่อขั้นต้น** — 3 ส.ค. user สั่งกลับมาลุยสายวิดีโอต่อ จึงขึ้นมาเป็นอันดับหนึ่ง
2. **§3 แบบ A (ตีเส้นแปลง)** — ได้ของที่คู่แข่งไม่มี ต้นทุนศูนย์ ใช้ทางเดิมของ overlay ที่มีอยู่
3. **§1 คลังคอนเทนต์** — แก้ปัญหาคลิปหมุนซ้ำใน 7 วัน
4. **§3.5/§3.6 ศูนย์รวมโพสต์+แชท+บอท LINE** — ฝั่ง "ส่งออก" (โพสต์คู่ Facebook/LINE ข้อความล้วน, ส่ง Rich Menu) เหลือแค่ต่อสาย ทำได้ก่อน · ฝั่ง "รับเข้า" (ปุ่มกด, แชท, CRM อัตโนมัติ) ต้องมีเซิร์ฟเวอร์สาธารณะก่อนทั้งคู่ จึงรวมเป็นก้อนใหญ่สุดของ roadmap ตอนนี้

## 5. กติกาที่ห้ามลืมตอนทำต่อ

- **แท็บที่ยังไม่เสร็จต้องกดไม่ได้** อย่าปล่อยลิงก์ตายไว้
- ทุกหน้าใหม่ต้องเพิ่ม producer ใน `src/publish/status.mjs` เพื่อให้หน้า `/status` ตรวจได้ด้วย
  โครงสร้างรองรับอยู่แล้ว เขียนฟังก์ชันเดียวจบ ไม่ต้องแตะ route หรือหน้าเว็บ
- **ไม่ผูกกับ CapCut** — ระบบ export ลงโฟลเดอร์แล้วจบ ไม่อ่าน/เขียนไฟล์โปรเจกต์ ไม่ import กลับ
- ของที่วาดเองได้ด้วย SVG อย่าไปเรียก AI สร้างภาพ — ทั้งถูกกว่าและตัวหนังสือไทยไม่เพี้ยน
