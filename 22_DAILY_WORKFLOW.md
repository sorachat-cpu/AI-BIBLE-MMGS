# 22. งานประจำวัน — ชีทที่ดิน · คอนเทนต์ความรู้ · คลิปวันละ 1

> Version: 1.0.0 · 2026-08-01
> ไฟล์นี้คือวิธีใช้จริงในแต่ละวัน · เรื่อง token ดูที่ `21_PUBLISH_SETUP.md`

---

## ตารางที่ตั้งไว้

| เวลา | วัน | อะไร | ไปไหน |
|---|---|---|---|
| **09:30** | **ทุกวัน** | **โพสต์ขายที่ดิน 1 แปลง** | **ชุดโพสต์เอง** → ลงกลุ่ม |
| 12:00 | จ · พ · ศ | คอนเทนต์ความรู้ (carousel) | เพจ |
| **19:00** | **ทุกวัน** | **คลิปสั้นแนวตั้ง** | FB Reels · TikTok · YouTube |

รวม 2 โพสต์/วัน (จ/พ/ศ เป็น 3) · มีทั้งคลิปและโพสต์ขายที่ดินทุกวันแน่นอน
ถ้าวันไหนหาของลงไม่ได้ ระบบจะ**เตือนเสียงดัง** ไม่ข้ามเงียบๆ

แก้เวลา/วัน/แพลตฟอร์มได้ที่ `content/schedule.config.json` ไม่ต้องแตะโค้ด

### กติกาการวนแปลง

- **แปลงเดียวกันห้ามลงซ้ำภายใน 24 ชม.** — กฎนี้ไม่มีการผ่อนผัน ต่อให้ทำให้ช่องนั้นว่าง
  เพราะที่ดินแปลงเดิมโผล่ซ้ำในวันเดียวกันในกลุ่มเดิม คือสิ่งที่ทำให้บัญชีโดนจำกัด
- **วนให้ครบทุกแปลงก่อนถึงจะกลับมาแปลงแรก** — เรียงตามแปลงที่ห่างจากการลงกลุ่มครั้งล่าสุดนานที่สุด
  ตอนนี้มี 11 แปลง = วนครบใน 11 วัน แล้วเริ่มรอบใหม่
- ยิ่งมีแปลงในชีทเยอะ ยิ่งวนห่าง เพิ่มแปลงได้เรื่อยๆ

---

## ครั้งแรก: เอาที่ดินที่เคยลงเพจเข้าชีท

### วิธี A — จาก Ads Manager (ทำไปแล้ว 11 แปลง ✅)

โพสต์ขายที่ดินของเพจถูกบูสต์เป็นโฆษณาไว้ Ads connector จึงอ่านข้อความโพสต์เหล่านั้นได้
โดยไม่ต้องมี Page token — เป็นทางที่ใช้ได้จริงกับเพจนี้ตอนนี้

```bash
npm run publish -- listings:from-ads    # อ่านจาก content/ads-creatives.json
```

> **ข้อจำกัด**: connector เป็น MCP ซึ่งเซิร์ฟเวอร์เรียกเองไม่ได้
> `content/ads-creatives.json` จึงเป็นจุดส่งต่อ — **มีประกาศใหม่เมื่อไหร่ บอก Claude ให้ดึงมาอัปเดตไฟล์นี้**
> หรือขอ Page token แล้วใช้วิธี C ซึ่งดึงเองได้อัตโนมัติ

### วิธี B — วางข้อความเอง (ใช้ได้เลย ไม่ต้องมี token)

เปิด http://localhost:3000/publish → หัวข้อ **ชีทที่ดิน** → กาง "วางข้อความโพสต์เก่าเอง"
คัดลอกโพสต์เก่าจากเพจมาวาง คั่นแต่ละโพสต์ด้วยบรรทัดที่มี `---`

```
ขายที่ดิน 2 ไร่ องครักษ์ นครนายก ถมแล้ว ติดถนนลาดยาง
ราคา 2.8 ล้าน เจ้าของขายเอง
แผนที่ https://maps.app.goo.gl/xxxxx
Line: @244raxjb
---
ขายที่ดิน 100 ตร.ว. ปากพลี ...
```

หรือทาง CLI:
```bash
npm run publish -- listings:import content/old-posts.txt
```

ระบบจะแยกให้เอง: ขนาด · ราคา · ทำเล · จุดเด่น · **ลิงก์แผนที่** · เบอร์ติดต่อ
แล้วยิง Google Places เติมสถานที่ใกล้เคียงให้ (~$0.001/โพสต์)

> คำโฆษณาเกินจริง (ทำเลทอง, ห้ามพลาด, ดีที่สุด) ถูกตัดออกอัตโนมัติ
> โพสต์ที่ไม่ใช่ประกาศขายที่ดินถูกข้ามเอง

### วิธี C — ดึงจากเพจอัตโนมัติ (ต้องมี token Facebook ก่อน)

```bash
npm run publish -- page:pull            # ดึงโพสต์เก่าจากเพจมาเก็บ
npm run publish -- listings:from-page   # แยกข้อมูลที่ดินจากโพสต์เหล่านั้น
```

วิธีนี้ได้**รูปของแปลงนั้นติดมาด้วย** ชุดโพสต์จึงมีรูปพร้อมเลย

---

## ชีทที่ดินมีอะไรบ้าง

`npm run publish -- listings:sheet` → ได้ `content/listings.csv` เอาไปเปิด Google Sheet ได้เลย

| คอลัมน์ | คือ |
|---|---|
| `listing_id` | รหัสแปลง (อย่าแก้ — ใช้จับคู่ตอนนำเข้ากลับ) |
| `status` | `NEW` · `READY` · `POSTED` · `SOLD` · `HIDDEN` |
| `size_text` / `price_text` | ขนาดและราคาตามที่เขียนไว้เดิม |
| **`maps_url`** | **ลิงก์แผนที่** — ตัวที่สำคัญที่สุด **ตอนนี้ว่างทั้ง 11 แปลง** เพราะโพสต์เดิมไม่มีลิงก์ ต้องเติมเอง |
| `nearby` | สถานที่ใกล้เคียง 3 อันดับจาก Places (ขึ้นได้ต่อเมื่อมี `maps_url`) |
| `caption` | แคปชั่นพร้อมโพสต์ แก้ตรงนี้ได้ |
| `last_group_post` | ลงกลุ่มครั้งล่าสุดเมื่อไหร่ |

แก้ในชีทเสร็จ → ดาวน์โหลดเป็น CSV → กด "นำเข้าชีทที่แก้แล้ว" หรือ:
```bash
npm run publish -- listings:read ~/Downloads/listings.csv
```

เติมลิงก์แผนที่เพิ่มแล้วสั่งเติมสถานที่ใกล้เคียงใหม่:
```bash
npm run publish -- listings:enrich
```

---

## "สร้างดราฟต์ให้เลยได้มั้ย" — ได้แค่ไหน

| ที่ไหน | ทำได้ไหม | หมายเหตุ |
|---|---|---|
| **ในระบบนี้** | ✅ **ทำอยู่แล้ว** | คิว + โฟลเดอร์ชุดโพสต์ พร้อม caption/รูป/คลิป |
| **ในเพจ Facebook** | ⏳ **โค้ดพร้อม รอ Page token** | ส่งเข้า Planner เป็นโพสต์ตั้งเวลา แก้ได้ กดโพสต์ก่อนเวลาได้ ลบได้ |
| **ในกลุ่ม Facebook** | ❌ **ไม่ได้ ไม่มีทาง** | Meta ไม่มี API ให้เลย ต้องวางเอง |

> **Facebook ไม่มี "ดราฟต์" ใน API เลย** — มีแค่โพสต์ตั้งเวลา (scheduled post) ซึ่งไปโผล่ใน
> Meta Business Suite → Planner ให้กดแก้/กดโพสต์ก่อน/ลบได้ก่อนถึงเวลา ซึ่งใช้งานได้เหมือนดราฟต์
> (`published=false` เฉยๆ จะกลายเป็น dark post ของฝั่งโฆษณา ไม่โผล่ใน Business Suite)

```bash
npm run publish -- schedule-fb --days 7           # ซ้อมก่อน
npm run publish -- schedule-fb --days 7 --live    # ส่งเข้า Planner จริง
```

ส่งเฉพาะช่องที่ลงเพจ (คลิป Reels + carousel) · ช่องลงกลุ่มไม่เกี่ยว ยังเป็นชุดโพสต์เองเหมือนเดิม
Facebook รับตั้งเวลาได้ **10 นาที ถึง 6 เดือน** ล่วงหน้าเท่านั้น ระบบเช็คให้ก่อนอัปโหลด

## แต่ละวันทำอะไร

```bash
npm run publish -- plan --days 7    # เตรียมล่วงหน้า 7 วัน (ทำสัปดาห์ละครั้งพอ)
npm run publish -- list             # ดูว่าเตรียมอะไรไว้บ้าง
```

เปิด http://localhost:3000/publish จะเห็นทั้งสัปดาห์ แก้แคปชั่นได้ในหน้าเลย

**โพสต์ขายที่ดินลงกลุ่ม** — กด "โพสต์จริง" ที่ช่องนั้น ระบบสร้างโฟลเดอร์ให้ใน
`output/post-kits/<วันที่>-<รหัสแปลง>/` มี `caption.txt` + `photo.jpg` (ถ้ามี)
เปิด caption.txt กด Cmd+A Cmd+C → วางในกลุ่ม → แนบรูป → กดโพสต์

โฟลเดอร์เดียวใช้ได้กับหลายกลุ่ม วางซ้ำได้เรื่อยๆ

**คลิปตอนเย็น** — ถ้าต่อ token แล้วจะยิงขึ้น 3 แพลตฟอร์มพร้อมกัน
ถ้ายังไม่ต่อ จะขึ้นว่า `ERR_PUB_03 ยังไม่ได้ตั้งค่า` ซึ่งเป็นแค่การข้าม ไม่ใช่ error

---

## คอนเทนต์ความรู้

6 หัวข้อที่เขียนไว้แล้ว:

| หัวข้อ | เรื่อง |
|---|---|
| `deed-types` | เอกสารสิทธิ์ที่ดิน 5 แบบ ต่างกันยังไง |
| `before-you-buy` | 6 ข้อต้องเช็คก่อนวางมัดจำ |
| `landlocked` | ที่ดินตาบอดคืออะไร ซื้อได้ไหม |
| `transfer-costs` | วันโอนที่ดินต้องจ่ายอะไรบ้าง |
| `city-plan-colours` | ผังเมืองสีอะไร สร้างอะไรได้ |
| `land-filling` | ถมที่ดินคิดยังไงให้ไม่บานปลาย |

```bash
npm run publish -- topics              # ดูรายการ
npm run publish -- carousel deed-types # สร้างสไลด์หัวข้อเดียว
npm run publish -- carousel:all        # สร้างใหม่ทุกหัวข้อ (หลังแก้ template)
npm run publish -- carousel:all --theme paper   # ธีมสว่าง
```

ได้ไฟล์ที่ `output/carousels/<หัวข้อ>/01.png ...` + `caption.txt`
โพสต์เป็นอัลบั้มหลายรูปในเพจ = carousel

**เพิ่มหัวข้อเอง**: แก้ `src/content/topics.mjs` คัดลอกโครงของหัวข้อเดิม
กติกา: หัวเรื่องไม่เกิน 3 บรรทัด · แต่ละข้อไม่เกิน 4 บรรทัด · **บรรทัดละไม่เกิน 40 ตัวอักษร**
(ภาษาไทยไม่มีช่องว่างระหว่างคำ ระบบตัดบรรทัดให้ไม่ได้ ต้องเขียนสั้นเอง)
`npm test` มีเทสต์คุมกติกานี้อยู่

**แก้หน้าตาสไลด์**: `templates/carousel-cover.svg` · `carousel-point.svg` · `carousel-cta.svg`
⚠️ ห้ามใส่ `--` ในคอมเมนต์ SVG (XML ไม่อนุญาต ทำให้ parse ไม่ผ่าน)

---

## ทำให้มันเดินเองทุกวัน

```bash
npm run daily                                       # เตรียม + จัดการช่องที่ถึงเวลา
npm run publish -- profile --set auto_publish=true  # ให้โพสต์เองจริงๆ
```

ตั้ง cron รายชั่วโมง (ดูวิธีใน `21_PUBLISH_SETUP.md` ท้ายไฟล์)

> `MANUAL_KIT` จะไม่โพสต์เองอยู่แล้วไม่ว่าตั้งค่าอะไร — มันได้แค่เตรียมโฟลเดอร์ให้
> เพราะ**กลุ่ม Facebook ไม่มี API ให้โพสต์อัตโนมัติ** ตั้งแต่ปี 2020

---

## เมื่อคลังคลิปใกล้หมด

```bash
npm run publish -- stock    # ดูว่าเหลือกี่คลิป พอกี่วัน
```

ถ้า "คลิปพอใช้" ต่ำกว่า 7 วัน ให้ไปเรนเดอร์เพิ่มที่ http://localhost:3000
คลิปใหม่ที่เรนเดอร์เสร็จจะเข้าคลังเองอัตโนมัติ ไม่ต้องทำอะไรเพิ่ม
